# PRD: Рестайл OG-баннера (граф контента + home-баннер)

**Автор:** Nikita Boyarkin
**Дата:** 2026-09-19
**Статус:** Draft — готов к исполнению (план согласован в грилинге 2026-09-19)
**Версия:** 1.0
**Проект:** `Personal_Projects.github.io` (Astro, RU/EN, палитра баннеров 60/30/10)
**Связанные документы:** `DESIGN.md` (§124, §117, §239, §259, §271–272), `docs/prd-palette-60-30-10.md`

---

## 0. Как читать этот документ

Это **execution PRD**: он фиксирует решения, принятые 2026-09-19, и превращает их в работу с
проверяемыми критериями приёмки. Правила:

1. **Единственный источник палитры баннеров** — константы в `scripts/generate-*-og.mjs`. Они не
   описаны в `DESIGN.md` (там другой бренд: teal/coral), поэтому этот PRD фиксирует их как
   данность и не переоткрывает.
2. **Критерий приёмки либо падает механически, либо явно помечен как ручной.**
3. **Классы доказательств:** `[V]` проверено чтением файла; `[I]` выведено из проверенного; `[NV]`
   не проверено. `[I]` никогда не повышается до `[V]`.
4. **Scope этого документа — только OG-баннеры.** Данные графа, `/graph/`, `graph.json`,
   постовые OG (24 шт.), темы сайта и hero-компоненты не входят (см. §4).

---

## 1. Executive Summary

Главная страница (RU и EN) отдаёт в соцсети превью `portfolio-graph-v1.png` — SVG-баннер 1200×630,
правая панель которого рисуется из **реального** контент-графа. Баннер собран изолированно от
дизайн-системы сайта: набран Helvetica Neue вместо Inter/Cormorant, не проходит WCAG AA по
оранжевому тексту, а правый граф в превью ~500px читается как «клубок» линий, а не структура.

Документ фиксирует рестайл: привести баннер к системному слою `DESIGN.md` (типографика, ритм,
радиусы, «accent семантичен»), починить AA-контраст, приглушить шум графа и выделить хабы —
не меняя палитру, пайплайн `rsvg-convert`, данные графа и размер. Вторым шагом тот же язык
переносится на `portfolio-banner-v2.png`, чтобы два баннера читались как серия.

---

## 2. Problem Statement

### 2.1 Текущая ситуация `[V]`

| Факт | Источник |
|---|---|
| Главные RU/EN отдают `og:image = /images/og/portfolio-graph-v1.png` | `src/pages/index.astro:69`, `src/pages/en/index.astro:69` |
| Fallback всех остальных страниц — `portfolio-banner-v2.png` | `src/layouts/Base.astro:32` |
| Баннеры генерируются вручную, в билд не вшиты | `package.json:17–21`; шапки скриптов; `.github/workflows/deploy.yml` OG-скрипты не вызывает |
| Шрифт баннера — `'Helvetica Neue', Helvetica, Arial` (fontconfig) | `scripts/generate-graph-og.mjs:80`, `scripts/generate-home-og.mjs:31` |
| Сайт использует Inter (variable 100–900) и Cormorant (variable 400–700), оба только `.woff2` | `src/styles/global.css:10,41`; `public/fonts/*.woff2` |
| Имя в hero на сайте — display-serif | `src/styles/global.css:827` (`font-family: var(--font-display)`), `src/styles/global.css:109–110` |
| OG-изображения не покрыты тестами и Lighthouse | grep по `tests/`, `.lighthouserc.json` — совпадений нет |

### 2.2 Три дефекта `[V]`

- **P1 — Типографика вне системы.** `rsvg-convert` не видит Inter/Cormorant: в fontconfig этих
  семейств нет, а `.woff2` fontconfig не читает. Баннер набран Helvetica, тогда как identity-шрифт
  сайта — Cormorant (`--font-display`).
- **P2 — Оранжевый текст не проходит AA.** Замеры на фоне `#1400c3`: CREAM `#f8f2da` — **10.29:1**,
  MUTED `#bdb5ea` — **6.03:1**, PERI `#9db4ff` — **5.72:1**, PEACH `#ffb38a` — **6.64:1**,
  ORANGE `#fe4e02` — **3.47:1** (только порог «крупного текста» 3:1). Роль «Product / Data Analyst»
  набрана 22px/600 — это **не** «крупный текст» (нужно ≥18.66px при 700 или ≥24px), значит правило
  AA 4.5:1 нарушено. `DESIGN.md:271` требует AA.
- **P3 — Правый граф нечитаем в превью.** Плотные тонкие рёбра `topic/shared/core` создают шум;
  обрезка по 3%..97% percentile оставляет дальние узлы у кромки, порождая длинные «случайные» линии
  (та голубая струна у нижнего края); нет акцента на хабах; поверхность графа не отделена от фона.

### 2.3 Влияние

- **Кто затронут:** рекрутёр/нанимающий, который видит превью в LinkedIn/X/Telegram; автор, чей
  бренд-сигнал размывается непрошедшим AA и «шумным» графом.
- **Как затронут:** превью ~500px не передаёт структуру портфолио — теряется ключевой нарратив
  «граф контента».
- **Серьёзность:** Medium. Функциональность не ломается, но страдает первый экран бренда и
  доступность.

### 2.4 Почему решать сейчас

Баннер уже используется как `og:image` главной; палитра стабильна; шрифты сайта —
самохостящиеся (путь вендоринга тривиален). Правка изолирована: 3 существующих файла + 1 новый
каталог ассетов.

---

## 3. Goals & Success Metrics

### Goal 1 — Типографика соответствует системе сайта
- **Метрика:** какие семейства фактически используются рендерером.
- **Baseline:** Helvetica Neue (`generate-graph-og.mjs:80`).
- **Target:** имя — Cormorant 600; служебный текст — Inter 400/500/600/700.
- **Метод:** рендер + визуальная сверка; `fc-match`/отсутствие warning'ов fontconfig.

### Goal 2 — Нет текста ниже WCAG AA
- **Метрика:** contrast ratio каждого текстового токена к фону `#1400c3`.
- **Baseline:** ORANGE-роль 3.47:1 — FAIL.
- **Target:** все текстовые элементы ≥4.5:1 (или ≥3:1 при явном «крупном» кегле/жирности).
- **Метод:** таблица контраста, печатаемая скриптом; ассерт в коде.

### Goal 3 — Граф читается как структура в превью ~500px
- **Метрика:** субъективная разборчивость + отсутствие артефактных линий.
- **Baseline:** «клубок», есть длинные случайные рёбра к выбросам.
- **Target:** рёбра `topic/shared/core` приглушены; хабы выделены; узлы degree ≤ 1 отброшены;
  поверхность графа отделена панелью.
- **Метод:** превью 1200px и даунскейл ~500px на ревью.

### Goal 4 — Два баннера читаются как серия
- **Метрика:** совпадение type/accent/surface-языка.
- **Target:** home-OG получает те же шрифты, CREAM-роль, AA и surface-правило; композиция
  (портрет-гексагон + retention-кривая) сохраняется.
- **Метод:** визуальная парная сверка.

---

## 4. Non-goals (явно вне scope)

- Данные графа: `buildGraph` / `layoutGraph` / `TOPICS` / `graph.json` / `/graph/` не меняются.
  Фильтр узлов в баннере — **presentation-only** (см. §6, REQ-05).
- Палитра баннеров остаётся `#1400c3 / #fe4e02 / #f8f2da` (60/30/10). Миграция в teal/coral
  `DESIGN.md` не производится.
  > **Пересмотрено 2026-09-19** (`prd-hero-banner-brand-polish.md`, P1): крем выравнен до
  > значения светлой темы `#f4efca` — сайт и баннеры теперь используют один крем. Синий
  > `#1400c3` и оранжевый `#fe4e02` сохранены как есть.
- Постовые OG (24 PNG, `scripts/generate-og.mjs`) не трогаем.
- On-site hero-компоненты (`HeroCta`, `HeroMetrics`, hero-разметка) не трогаем.
- CI-гард на рендер OG не добавляем (см. §7).
- Размер 1200×630, мета-теги, роль «role=accent» не меняются.
- Гексагон `NB`, URL `nikitaboyarkin.github.io`, имя «Nikita Boyarkin», подпись
  «Product / Data Analyst» сохраняются.

---

## 5. Принятые решения (грилинг 2026-09-19)

| ID | Решение |
|---|---|
| D1 | Целевая рамка — системный слой `DESIGN.md` (типо-дисциплина, «accent семантичен, не декоративен», шкала радиусов, бюджет 60/30/10), **палитра баннера сохраняется** |
| D2 | Scope шага 1 — `generate-graph-og.mjs`; home-OG — сразу следом |
| D3 | Жёсткие ограничения сохранены: 1200×630, `rsvg-convert` (без новых зависимостей и headless-браузера), реальный граф, палитра, гексагон/URL/имя/роль |
| D4 | Шрифты вендорим как **статические TTF** — Inter 400/500/600/700, Cormorant 600/700 — в `scripts/og-fonts/`, отдаём рендереру через `FONTCONFIG_FILE` + локальный `fonts.conf` |
| D5 | Композиция — перебаланс (не пересборка): панель графа с клипом, рамкой, vignette и приглушённым glow; один крупный гексагон-водяной знак; ритм колонки по 8px; Cormorant только для имени |
| D6 | Читаемость графа: приглушить `topic/shared/core`; акцент на хабах; отбросить degree ≤ 1; контраст узлов ↑; подписи якорей — эксперимент с проверкой на 500px |
| D7 | Выход переименовывается в `portfolio-graph-v2.png`, обновляются 2 ссылки |
| D8 | Роль — CREAM; оранжевый остаётся семантическим акцентом (правила, точки легенды, подчёркивание, верхняя полоса, рёбра) |
| D9 | Фильтр графа — presentation-only, документируется комментарием в скрипте |
| D10 | Home-OG: (D4 + D8 + AA + ритм + surface-язык), свой hero-визуал; слияние в один шаблон не делаем |
| D11 | Формат сдачи: рендер → превью 1200 и ~500px → таблица контраста → правка ссылок; `v1` удаляется после одобрения; коммит только по явной команде |
| D12 | Контраст-ассерт внутри OG-скриптов — обязателен |
| D13 | Фиксация: комментарий-шапка в скриптах + строка в `CLAUDE.md` про `scripts/og-fonts/`; ADR не создаётся |
| D14 | CI-гард на OG-дрейф не добавляется |

---

## 6. Requirements

Обозначения: **P0** — must have, **P1** — should have, **P2** — nice to have.

### REQ-01 (P0) — Шрифтовой слой OG
Статические TTF Inter (400/500/600/700) и Cormorant (600/700) лежат в `scripts/og-fonts/` вместе с
`fonts.conf` и `OFL.txt`. Скрипты выставляют `FONTCONFIG_FILE` при вызове `rsvg-convert`.
- **Критерий:** рендер баннера даёт Cormorant в имени и Inter в служебном тексте (визуальная
  сверка), в выводе нет fontconfig-fallback warning'ов.

### REQ-02 (P0) — Типографическая раскладка граф-баннера
- Имя «Nikita Boyarkin» — Cormorant 600, ~60px, letter-spacing ≈ −0.5.
- Роль «Product / Data Analyst» — Inter 600, **CREAM**, ~24px.
- Секция «ГРАФ КОНТЕНТА» — Inter 700, letter-spacing ≈ 3px, CREAM, ~15px.
- Дескриптор (2 строки) — Inter 400/500, MUTED, ~15px, lh ≈ 1.4.
- Легенда — Inter 500, MUTED, 16px; точки сохраняют цвета категорий.
- URL — Inter 500, CREAM, ~22px.
- **Критерий:** перечисленные пары (роль/кегль) присутствуют в SVG-разметке скрипта.

### REQ-03 (P0) — WCAG AA для всего текста
- **Критерий (механический):** скрипт бросает ошибку, если contrast ratio любого текстового токена
  к фону < 4.5:1 (или < 3:1 при явно «крупном» кегле). Эталонные замеры: CREAM 10.29, MUTED 6.03,
  PERI 5.72, PEACH 6.64, ORANGE 3.47.

### REQ-04 (P0) — Поверхность граф-панели
- Клип радиус 20; рамка 1px (край ≈ 0.14); мягкая внутренняя vignette (края темнее центра);
  оранжевый radial glow приглушён до ≤0.08.
- **Критерий:** атрибуты рамки/vignette присутствуют в SVG; glow ≤0.08.

### REQ-05 (P0) — Читаемость графа (presentation-only)
- Рёбра `topic/shared/core` приглушены; `related/children` остаются проминентными.
- Топ-узлы по degree визуально акцентированы (размер и/или обводка).
- Узлы с degree ≤ 1 / выбросы отбрасываются **в статичной картинке**; данные графа не меняются.
- **Критерий:** в скрипте есть явный фильтр и комментарий «presentation-only»; `buildGraph`,
  `layoutGraph`, `TOPICS` не изменены (diff пуст).

### REQ-06 (P1) — Подписи якорных узлов
- 3–5 подписей топ-хабов — только если читаемы в превью ~500px.
- **Критерий:** решение зафиксировано по результату визуальной проверки; при нечитаемости —
  не включаются (и это отмечается в §10).

### REQ-07 (P0) — Артефакты и ссылки
- Выход граф-баннера — `public/images/og/portfolio-graph-v2.png`.
- Ссылки обновлены: `src/pages/index.astro:69`, `src/pages/en/index.astro:69`.
- **Критерий:** `grep -rn "portfolio-graph-v1" src/` пуст; новый файл существует, размеры 1200×630.

### REQ-08 (P0) — Home-OG приведён к серии
- `scripts/generate-home-og.mjs` использует те же шрифты, CREAM-роль, AA-ассерт и surface-правило;
  композиция (портрет-гексагон + retention-кривая) сохраняется.
- **Критерий:** оба скрипта проходят с одним `fonts.conf`; AA-ассерт зелёный.

### REQ-09 (P1) — Документация
- Комментарий-шапка в обоих OG-скриптах описывает пайплайн шрифтов.
- Одна строка в `CLAUDE.md` про `scripts/og-fonts/` и `FONTCONFIG_FILE`.

---

## 7. Technical Considerations

### 7.1 Пайплайн шрифтов (ключевой риск)
`rsvg-convert` использует fontconfig и **не поддерживает** `@font-face`/`.woff2`. Решение: локальный
`fonts.conf` с `<dir>` на `scripts/og-fonts/` + `FONTCONFIG_FILE` в окружении `spawnSync`.
Альтернатива (отклонена по D3): смена рендерера — в системе есть `resvg`, `sharp`, `playwright`,
но это нарушает «без новых зависимостей и headless-браузера».

### 7.2 Почему статические TTF, а не variable
Оба шрифта на сайте — variable (`global.css:10,41`), но pango/`rsvg-convert` не гарантирует выбор
веса из variable-оси. Статические начертания детерминированы (D4). Сабсетинг — опционально.

### 7.3 Общий хелпер
Шрифтовая обвязка + контраст-ассерт нужны трём OG-скриптам. Кандидат: `scripts/lib/og-render.mjs`.
Решение — implementation detail, может быть отклонено в пользу дублирования ~15 строк.

### 7.4 Совместимость
`rsvg-convert 2.62.3` — единственный обязательный внешний бинарь; документируется в шапке скрипта
(сейчас нигде не задокументирован).

---

## 8. Work Plan

**Шаг 0 — шрифтовой спайк.** Проверить, что `rsvg-convert` рисует Inter/Cormorant из
`scripts/og-fonts/` через `FONTCONFIG_FILE`. Блокирует всё остальное.

**Шаг 1 — ассеты.** `scripts/og-fonts/`: `inter-{400,500,600,700}.ttf`, `cormorant-{600,700}.ttf`,
`fonts.conf`, `OFL.txt`.

**Шаг 2 — граф-баннер.** `scripts/generate-graph-og.mjs`: REQ-01…REQ-07, REQ-09.

**Шаг 3 — home-баннер.** `scripts/generate-home-og.mjs`: REQ-01, REQ-03, REQ-08, REQ-09.

**Шаг 4 — ссылки и доки.** `src/pages/index.astro`, `src/pages/en/index.astro`, `CLAUDE.md`.

**Шаг 5 — проверка.** `bun run og:graph`, `bun run og:home`, `bun run check`; превью 1200 и ~500px;
таблица контраста. Удаление `portfolio-graph-v1.png` — после одобрения.

---

## 9. Acceptance Metrics

| # | Критерий | Тип |
|---|---|---|
| A1 | `bun run og:graph` и `bun run og:home` завершаются с кодом 0 | механический |
| A2 | Контраст-ассерт не падает на токенах REQ-02 | механический |
| A3 | `grep -rn "portfolio-graph-v1" src/` — пусто | механический |
| A4 | `portfolio-graph-v2.png` существует, 1200×630 | механический |
| A5 | `git diff src/lib/graph.ts src/lib/graph-layout.ts src/lib/topics.ts` — пусто | механический |
| A6 | Имя набрано Cormorant, служебный текст — Inter | ручной (визуальная сверка) |
| A7 | В превью ~500px граф читается структурой, нет артефактных линий | ручной |
| A8 | Граф-баннер и home-баннер читаются как одна серия | ручной |
| A9 | `bun run check` — зелёный | механический |

---

## 10. Risks

| Риск | Вероятность | Митигация |
|---|---|---|
| fontconfig не подхватывает TTF через `FONTCONFIG_FILE` | Средняя | Шаг 0 блокирует; при провале — вернуться к выбору рендерера/шрифтов, не нарушая D3 |
| Cormorant с маленьким x-height «теряет» имя | Средняя | Кегль ~60px вместо 46 + визуальная проверка |
| Точки легенды/узлы сливаются на синем | Низкая | Контраст узлов ↑ (REQ-05), обводка фоном |
| Подписи якорей нечитаемы в превью | Средняя | REQ-06: не включать по результату проверки |
| Расхождение фиксированных вручную PNG с данными графа | Низкая | Скрипт строит из того же `buildGraph`; напоминание в шапке |
| +~1 МБ бинарных TTF в репо | Низкая | Сабсетинг при необходимости; ассеты вне `public/` |
| Нечитаемость длинного оранжевого «Product / Data Analyst» после смены на CREAM | Низкая | Иерархию держим весом/кеглем (D8) |

---

## 11. PENDING — нужно от владельца

- **P1:** ~~принять или отклонить общий хелпер `scripts/lib/og-render.mjs` (§7.3)~~ → **принят** (§14).
- **P2:** ~~включать ли подписи якорных узлов (REQ-06)~~ → **отключены**: нечитаемы на ~500px (§14).
- **P3:** ~~удалять ли `portfolio-graph-v1.png` после одобрения (D11)~~ → **удалён** (одобрено 2026-09-19).
- **P4:** разрешение на коммит (D11 — только по явной команде) — **открыт**.

---

## 12. Appendix — инвентарь файлов

### Изменяемые существующие
| Файл | Что |
|---|---|
| `scripts/generate-graph-og.mjs` | REQ-01…REQ-07, REQ-09 — основная правка |
| `scripts/generate-home-og.mjs` | REQ-01, REQ-03, REQ-08, REQ-09 |
| `src/pages/index.astro` | строка `og:image` → v2 |
| `src/pages/en/index.astro` | строка `og:image` → v2 |
| `CLAUDE.md` | строка про `scripts/og-fonts/` |

### Новые
| Путь | Что |
|---|---|
| `scripts/og-fonts/inter-400.ttf`, `inter-500.ttf`, `inter-600.ttf`, `inter-700.ttf` | Inter (OFL) |
| `scripts/og-fonts/cormorant-600.ttf`, `cormorant-700.ttf` | Cormorant (OFL) |
| `scripts/og-fonts/fonts.conf` | fontconfig для рендера |
| `scripts/og-fonts/OFL.txt` | лицензия |
| `scripts/lib/og-render.mjs` (`[P1]`) | опциональный общий хелпер |

### Генерируемые артефакты
| Путь | Что |
|---|---|
| `public/images/og/portfolio-graph-v2.png` | новый выход |
| `public/images/og/portfolio-banner-v2.png` | перезапись (имя сохраняется) |
| `public/images/og/portfolio-graph-v1.png` | удалён (одобрено 2026-09-19, D11) |

### Не трогаем
`src/lib/graph.ts`, `src/lib/graph-layout.ts`, `src/lib/topics.ts`, `graph.json`, `/graph/`,
`scripts/generate-og.mjs`, постовые OG, `src/layouts/Base.astro` (fallback),
hero-компоненты, `.lighthouserc.json`.

---

## 13. Проверенные факты, на которые опирается документ `[V]`

- `rsvg-convert 2.62.3` установлен; также доступны `resvg`, `magick`, `convert`; в
  `node_modules` — `sharp`, `playwright`.
- В fontconfig нет семейства Inter; Inter/Cormorant присутствуют только как `.woff2`.
- Контраст к `#1400c3`: CREAM 10.29, MUTED 6.03, PERI 5.72, PEACH 6.64, ORANGE 3.47.
- OG-скрипты не вызываются из билда/CI; OG-тестов нет.
- В `public/images/og/` существуют `portfolio-banner-v2.png` и `portfolio-graph-v1.png`; ранее
  существовал `portfolio-banner.png` (удалён из индекса).

---

## 14. Execution log

**2026-09-19 — Шаги 0–5 выполнены.** Все механические критерии зелёные (см. таблицу ниже).
`portfolio-graph-v1.png` удалён (P3 одобрен); коммит не делался (P4).

### Шаг 0 — шрифтовой спайк `[V]`

`FONTCONFIG_FILE` **сам по себе не работает** на macOS: `rsvg-convert 2.62.3 → pango
1.58 → cairo` по умолчанию использует бэкенд **CoreText**, который полностью игнорирует
fontconfig. Проверено: с пустым fontconfig (`0` шрифтов) pango всё равно рисовал текст
системным sans. Решение (в рамках D3, без новых зависимостей):

```
FONTCONFIG_FILE    = scripts/og-fonts/fonts.conf
PANGOCAIRO_BACKEND = fc
```

С `PANGOCAIRO_BACKEND=fc` пустой конфиг даёт пустой вывод (config реально читается), а
Cormorant рисуется засечным — спайк пройден. Дополнительно: fontconfig резолвит
относительный `<dir>` относительно **cwd**, поэтому рендер запускается с
`cwd = scripts/og-fonts` (`prefix="cwd"`, чтобы не было warning'а об ambiguous path).

### Шаги 1–3 — ассеты и баннеры `[V]`

- **Ассеты.** `scripts/og-fonts/`: статические TTF Inter 400/500/600/700 и Cormorant
  600/700 (полные, без variable-осей, Latin+Cyrillic), `fonts.conf`, `OFL.txt`
  (обе лицензии). Имена в `name`-таблице **нормализованы**: Google отдаёт 600 как
  отдельное семейство «Inter SemiBold», которое fontconfig не матчит как weight=600 —
  семейство сведено к `Inter`/`Cormorant`, вес — через `usWeightClass` и subfamily.
- **REQ-02/REQ-04/REQ-05** — переписан `generate-graph-og.mjs`: Cormorant 60/600 для
  имени, Inter для служебного текста; роль → CREAM; клип r=20 + рамка 1px@0.14 +
  vignette + glow 0.08; рёбра `topic/shared/core` приглушены (0.06–0.08), `related`/
  `children` проминентны; хабы (топ-6 по degree) получают оранжевый halo. Фильтр кадра
  — presentation-only: degree ≥ 2, затем срез дальних ~16% по радиусу от центроида
  (лейаут валит слабо связанную группу на границу мира — 5 узлов стопкой в одну
  точку). Рёбра длиннее ~0.42·диагонали панели растворяются по длине. Итог: 45/56
  узлов, 147 рёбер.
- **REQ-06 (P2) — решено: подписи якорей НЕ включаются.** Проверено рендером с
  `ANCHOR_LABELS = true`: на ~500px подписи 12px сжимаются до ~5px, налезают друг на
  друга и читаются как шум. Код оставлен за флагом `ANCHOR_LABELS = false` (с
  комментарием) для крупноформатного рендера.
- **REQ-03/REQ-08** — переписан `generate-home-og.mjs`: Cormorant для имени, Inter для
  остального, роль → CREAM; AA-ассерт; композиция (портрет-гексагон + retention-кривая)
  сохранена, кривая завернута в ту же поверхность (clip + рамка 0.14 + vignette + glow).
- **REQ-07/REQ-09** — выход `portfolio-graph-v2.png`; ссылки обновлены в
  `src/pages/index.astro` и `src/pages/en/index.astro`; шапки обоих скриптов описывают
  пайплайн шрифтов; в `CLAUDE.md` добавлена строка про `scripts/og-fonts/` +
  `FONTCONFIG_FILE`/`PANGOCAIRO_BACKEND`.

### Принятые решения по PENDING

- **P1 — общий хелпер принят:** `scripts/lib/og-render.mjs` (обвязка шрифтов,
  `renderSvgToPng`, WCAG-контраст-ассерт). Потребовал `!scripts/lib/` в `.gitignore`
  (generic `lib/` иначе скрывает каталог). Дублирование ~15 строк отклонено.
- **P2 — подписи якорей отключены** (см. REQ-06 выше).
- **P3 — `portfolio-graph-v1.png` удалён** по одобрению владельца (2026-09-19).
- **P4 — коммит не делался** (только по явной команде).

### Таблица контраста (bg `#1400c3`) `[V]`

| Токен | Цвет | Ratio | Кегль | Итог |
|---|---|---|---|---|
| name (graph) | CREAM | 10.29:1 | 60/600 | PASS |
| role | CREAM | 10.29:1 | 24/600 | PASS |
| caption «ГРАФ КОНТЕНТА» | CREAM | 10.29:1 | 15/700 | PASS |
| descriptor | MUTED | 6.03:1 | 15/400 | PASS |
| legend | MUTED | 6.03:1 | 16/500 | PASS |
| url | CREAM | 10.29:1 | 22/500 | PASS |
| ORANGE (точка легенды, ring) | ORANGE | 3.47:1 | — | graphic (skip) |

### Acceptance Metrics `[V]`

| # | Критерий | Результат |
|---|---|---|
| A1 | `og:graph` + `og:home` exit 0 | PASS |
| A2 | контраст-ассерт не падает | PASS (6 токенов) |
| A3 | `grep -rn "portfolio-graph-v1" src/` пусто | PASS |
| A4 | v2 существует, 1200×630 | PASS |
| A5 | diff `graph.ts`/`graph-layout.ts`/`topics.ts` пуст | PASS |
| A6 | имя — Cormorant, служебный текст — Inter | PASS (визуально) |
| A7 | ~500px читается структурой, нет артефактных линий | PASS (визуально; длинные рёбра растворены) |
| A8 | два баннера — одна серия | PASS (визуально) |
| A9 | `bun run check` зелёный | PASS (0 errors/warnings/hints) |

Дополнительно: `rsvg-convert` в проде-окружении рисует без fontconfig-fallback
warning'ов (stderr пуст, `[V]`).

### Ревизия композиции (2026-09-19, по запросу владельца)

Главный OG переделан в «граф слева + портрет справа» (было: идентичность слева,
граф справа):

- **Портрет** — та же гексагонная геометрия, что у fallback-баннера
  (`portfolio-banner-v2.png`), поэтому оба баннера читаются как серия.
- **Идентичность** — компактная шапка сверху слева: логотип, имя (Cormorant 52/600),
  роль (Inter 22/600, CREAM), оранжевая линейка.
- **Граф** — панель 692×344 слева; подпись «ГРАФ КОНТЕНТА» над панелью; легенда —
  горизонтальной строкой под графом; URL внизу слева.
- Двухстрочный дескриптор убран (нет места в новой композиции); рёбра дополнительно
  растворяются по длине (`EDGE_FADE_START = 0.3·диагонали`); срез выбросов
  `RADIAL_KEEP = 0.72`; узлы чуть крупнее (`RADIUS_BOOST = 1.15`).
- Итог: 42/56 узлов, 132 ребра; AA-ассерт обновлён и зелёный
  (name/role/logo/caption/legend/url).
- REQ-02/REQ-06 в части кеглей и вертикальной раскладки колонки перекрыты этой
  ревизией. Fallback-баннер (`portfolio-banner-v2.png`) не менялся.

### Открытые пункты

- **P4:** коммит (только по явной команде).

