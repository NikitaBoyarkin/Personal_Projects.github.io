# PRD: Сквозная полировка бренда — OG-баннеры + живой герой

**Автор:** Nikita Boyarkin
**Дата:** 2026-09-19
**Статус:** Draft — план согласован в грилинге 2026-09-19, ожидает подтверждения владельца перед исполнением
**Версия:** 1.0
**Проект:** `Personal_Projects.github.io` (Astro 7, чистый CSS, RU/EN, три темы)
**Связанные документы:** `docs/prd-og-banner-restyle.md` (исполнен 2026-09-19), `docs/prd-palette-60-30-10.md`, `docs/prd-readability.md`, `DESIGN.md`

---

## 0. Как читать этот документ

Это **execution PRD**: он фиксирует решения грилинга 2026-09-19 и превращает их в работу с
проверяемыми критериями приёмки.

1. **Классы доказательств:** `[V]` — проверено чтением файла; `[I]` — выведено из проверенного;
   `[NV]` — не проверено. `[I]` никогда не повышается до `[V]`.
2. **Критерий приёмки либо падает механически, либо явно помечен как ручной.**
3. **Scope — четыре OG-баннера + живой герой + единый источник бренд-палитры.** Данные графа,
   постовые OG (24 шт.), три темы сайта, структура страниц и H1-семантика не входят (§4).
4. Этот PRD **пересматривает часть non-goals** исполненного `prd-og-banner-restyle.md` — см. §2.5.

---

## 1. Executive Summary

Живой сайт и его OG/маркетинговые баннеры используют **одну палитру, но перевёрнуто**, и нигде не
зафиксированы как одна система. Баннерный синий `#1400c3` — это ровно `--button-bg` сайта; баннерный
крем `#f8f2da` — почти светлый фон `#f4efca`; баннерный оранжевый `#fe4e02` и сайтовый коралл
`#ff8569` — соседние, но не равные hue. При этом **единого источника палитры нет**: хексы захардкожены
в каждом OG-скрипте, а `global.css` держит токены отдельно, поэтому поверхности неизбежно расходятся.

Документ закрывает три вещи:

1. **Единый источник бренда** — `src/lib/brand.ts` с двумя контекстными акцентами и одной
   геометрией гексагона, который импортируют и OG-скрипты, и сайт (через токены + drift-тест).
2. **Герой** — полировка без смены идентичности: типографика цифр, вертикальный ритм, CTA-ряд,
   чистка мёртвого CSS.
3. **Четыре баннера** — CV-пара глубоко (артефакт-подчёркивание, стат-блок, гексагон, фон, сетка),
   home/graph точечно (единый гексагон и типографика — они только что отрестайлены).

Направление — **сохранить идентичность, вычистить детали**, а не редизайн.

---

## 2. Problem Statement

### 2.1 Текущая ситуация `[V]`

| Факт | Источник |
|---|---|
| OG-скрипты рендерятся `rsvg-convert` (librsvg 2.62.3) через общий `scripts/lib/og-render.mjs` | `scripts/lib/og-render.mjs:56–73`; `docs/prd-og-banner-restyle.md:340–356` |
| Палитра баннеров захардкожена в каждом скрипте; общего модуля нет | `generate-cv-og.mjs:32–34`, `generate-cv-linkedin.mjs:31–33`, `generate-home-og.mjs:33–35`, `generate-graph-og.mjs:53–58` |
| Единственный хекс, общий для скриптов и сайта — `#1400c3` (`--button-bg`) | `global.css:100` (dark), `:172` (light); скрипты — см. выше |
| Баннерный оранжевый `#fe4e02` не встречается в `src/` вообще | grep по `src/` — 0 совпадений |
| Кремы расходятся: баннер `#f8f2da` vs light-тема `#f4efca` | `generate-*.mjs`; `global.css:153` |
| Геометрия гексагона задана по-разному: в `cv-og`/`home`/`graph` — захардкоженные вершины, в `cv-linkedin` — вычисляемые | `generate-cv-og.mjs:83–85` vs `generate-cv-linkedin.mjs:55–73` |
| Цифры героя набраны `--font-mono` (SF Mono/Monaco), а цифры баннера — Inter | `HeroMetrics.astro:121–128`; `generate-cv-og.mjs:76–77` |
| Между метриками героя нет разделителей; в баннере разделители есть | `HeroMetrics.astro:109–143` (нет); `generate-cv-og.mjs:72–75` (есть) |
| Единицы расходятся: «п.п.» в баннере, «pp» в герое | `generate-cv-og.mjs:58`; `src/lib/metrics.ts` + `HeroMetrics.astro` |
| Мёртвый CSS `#intro-shader`, `.hero-viz-cards`, `.hero-viz-card` + неиспользуемый `IntroShader.astro` | `global.css:536–551, 906–908, 911–944`; `index.astro` не импортирует `IntroShader` |
| Под героем в OG-баннерах лежит артефакт — оранжевая линейка 120×3 px | `generate-cv-og.mjs:139`, `generate-cv-linkedin.mjs:152` |
| В `cv-og`/`cv-linkedin` строки стат-блока разнесены фиксированными шагами, без единого ритма | `generate-cv-og.mjs:63–68`; `generate-cv-linkedin.mjs:83–88` |
| Контраст-ассерт для шрифта баннеров уже есть (обёртка `assertContrast`) | `scripts/lib/og-render.mjs:79–140` |

### 2.2 Дефекты

- **B1 — Hero-цифры вне системы.** Одни и те же метрики набраны mono в герое и Inter в баннере;
  на 1.25rem mono выглядит случайным и ломает серию. `[V]`
- **B2 — Нет ритма в hero.** Линейный `gap: 1rem` между аватаром, H1, ролью, питчем, PhD-строкой и
  метриками не отделяет смысловые блоки от визуальных. `[V]`
- **B3 — CTA-ряд из трёх кнопок** (primary + «Связаться» + «CV») без явной иерархии и мобильной
  упаковки. `[V]`
- **B4 — Мёртвый CSS/компонент** — ~30 строк и один `.astro` без разметки. `[V]`
- **B5 — Артефакт-подчёркивание** 120×3 px под карточкой статов в CV-паре читается как случайный
  элемент. `[V]`
- **B6 — Стат-блок CV-пары** не имеет единого вертикального ритма и разделителей на уровне
  контейнера; бледный фон панели теряется. `[V]`
- **B7 — Гексагон-портрет** имеет кремовую заливку и узкую обрезку, конфликтует с синим фоном. `[V]`
- **B8 — Фон баннеров** плоский: синий без глубины, декор-шестиугольники слабые. `[V]`
- **B9 — Бренд-марка NB и пилюля CV · PDF** без явной оптической иерархии относительно имени. `[I]`
- **B10 — Дрейф палитры неизбежен**: общего источника нет, значения расходятся уже сейчас (кремы,
  оранжевый/коралл, геометрия гексагона). `[V]`

### 2.3 Влияние

- **Кто затронут:** рекрутёр/нанимающий, видящий превью и первый экран; автор, чей бренд-сигнал
  размывается расхождением поверхностей.
- **Как затронут:** баннер и сайт не читаются как одна система; мелкие визуальные шумы (B4–B7)
  снижают воспринимаемое качество.
- **Серьёзность:** Medium — функциональность не ломается, страдает целостность бренда.

### 2.4 Почему сейчас

Оба баннера только что получили общий шрифтовой слой и AA (`prd-og-banner-restyle.md` — исполнен);
палитра стабильна; `og-render.mjs` уже существует как естественная точка входа для бренд-модуля.
Правка изолирована: 1 новый модуль + 1 тест + правки в существующих файлах.

### 2.5 Отношение к `prd-og-banner-restyle.md` (пересмотр non-goals)

| Решение исполненного PRD | Статус здесь |
|---|---|
| §4 «Палитра баннеров остаётся `#1400c3/#fe4e02/#f8f2da`» | **Частично пересматривается:** ORANGE сохраняется как контекстный акцент, CREAM выравнивается до одного значения (см. REQ-03, PENDING P1) |
| §4 «On-site hero-компоненты не трогаем» | **Отменяется** — hero в scope (REQ-09…REQ-13) |
| §4 «CI-гард на рендер OG не добавляем» | **Не отменяется** — drift-gate проверяет токены CSS vs бренд-модуль, а не рендер OG (REQ-02) |
| §4 «Постовые OG (24 PNG) не трогаем» | **Сохраняется** |
| §4 «Данные графа (`buildGraph`/`layoutGraph`/`TOPICS`) не меняются» | **Сохраняется** |
| D10 «Имя/URL/роль/гексагон сохраняются» | Имя, URL, роль — сохраняются; геометрия гексагона унифицируется (REQ-05) |
| D11 «Формат сдачи: рендер → превью 1200/~500 → контраст → ссылки» | **Наследуется** |
| D12 «Контраст-ассерт внутри OG-скриптов» | **Наследуется и распространяется на CV-пару** |

---

## 3. Goals & Success Metrics

### Goal 1 — Один источник бренд-палитры
- **Метрика:** количество мест, где бренд-хекс задан вне `brand.ts`.
- **Baseline:** 4 OG-скрипта × локальные константы + `global.css` (расхождение крема/акцента). `[V]`
- **Target:** 0 «голых» бренд-хексов в OG-скриптах; `global.css` совпадает с `brand.ts` (drift-тест).
- **Метод:** импорт модуля в скриптах + `tests/lib/brand.test.ts`.

### Goal 2 — Герой и баннеры читаются как одна серия
- **Метрика:** совпадение семейства цифр, единиц и разделителей.
- **Baseline:** mono vs Inter; «pp» vs «п.п.»; разделители только в баннере. `[V]`
- **Target:** Inter + `tabular-nums` в обоих; единые единицы; разделители в обоих.
- **Метод:** визуальная парная сверка + чтение CSS.

### Goal 3 — Ноль визуальных артефактов в CV-паре
- **Метрика:** артефакт-подчёркивание, ритм стат-блока, гексагон.
- **Baseline:** линейка 120×3; фиксированные шаги; кремовая заливка. `[V]`
- **Target:** линейка заменена осознанным разделителем или убрана; единый ритм; гексагон без
  кремовой заливки, на градиенте.
- **Метод:** рендер + превью 1200 и ~500 px.

### Goal 4 — WCAG AA не регрессирует
- **Метрика:** contrast ratio текстовых токенов в баннерах и токенов hero в трёх темах.
- **Baseline:** баннеры — AA-ассерт зелёный (`prd-og-banner-restyle.md:393–403`). `[V]`
- **Target:** все текстовые пары ≥4.5:1 (или ≥3:1 при явном large); contrast-gate hook зелёный.
- **Метод:** контраст-ассерт в скриптах + `contrast-gate.js` на правках `global.css`.

### Goal 5 — Ничего не сломано
- **Метрика:** типы, тесты, Lighthouse.
- **Target:** `bun run check` зелёный, `bun test` зелёный, LHCI a11y/SEO/BP ≥ 0.95, perf ≥ 0.85.
- **Метод:** команды ниже (§9).

---

## 4. Non-goals (явно вне scope)

- Данные графа, `graph.json`, `/graph/`, `src/lib/graph*.ts`, `src/lib/topics.ts` — не меняются.
- Постовые OG (24 PNG, `scripts/generate-og.mjs`) — не трогаем.
- Три темы (dark/light/cyberpunk) сохраняются; четвёртая не вводится.
- H1 героя остаётся role-first («Продуктовый аналитик» / «Product Analyst») — см. D9.
- Два набора статов различаются осознанно: герой — 4 контекстных, баннер — 3 impact.
- Смена рендерера (`rsvg-convert`) и добавление новых зависимостей не производится.
- Коммит — только по явной команде владельца.
- `design-plans/*.md` не создаётся (формат сдачи — план в чате + правки + до/после рендеры, D7).

---

## 5. Принятые решения (грилинг 2026-09-19)

| ID | Решение |
|---|---|
| D1 | Scope — **баннеры + живой герой** как один бренд (не только баннер) |
| D2 | Направление — **полировка с сохранением идентичности**, не редизайн |
| D3 | Инварианты: три темы, WCAG-AA hook, LHCI (a11y/SEO/BP ≥ 0.95, perf ≥ 0.85), `DESIGN.md` — источник истины, палитра 60-30-10 |
| D4 | Двойной режим: бирюза — канвас сайта, синий — сигнальный цвет маркетинга/CTA; связь через типографику, геометрию гексагона и сетку |
| D5 | Гексагон — бренд-марк; фото на сайте — круглое; в OG допускается гексагонная обрезка, но без кремовой заливки, на градиенте |
| D6 | Правки баннеров: весь список B5–B9; порядок — сетка/воздух и стат-блок, затем фон/фото/марка, затем типографика и пилюля |
| D7 | Формат сдачи: план в чате + правки + рендеры до/после; `design-plans/` файл не создаём |
| D8 | В scope все четыре баннера: CV и CV-LinkedIn глубоко, home/graph точечно |
| D9 | H1 героя остаётся role-first; «один бренд» достигается системой, а не порядком строк |
| D10 | Акценты — два контекстных (высокохромный оранжевый для синего, коралл для бирюзы), оба из одного источника; крем выравнивается до одного значения |
| D11 | Цифры героя → Inter + `tabular-nums`, лёгкие разделители, единые единицы; два набора статов сохраняются |
| D12 | Мёртвый CSS `#intro-shader`/`.hero-viz-*` и `IntroShader.astro` удаляются |
| D13 | Единый источник — `src/lib/brand.ts`; `global.css` остаётся рукописным, совпадение токенов гарантирует drift-тест |
| D14 | Герой-полировка: сначала ритм и CTA; аватар/панель/питч — только при подтверждённой проблеме в рендере |

---

## 6. Requirements

Обозначения: **P0** — must have, **P1** — should have, **P2** — nice to have.

### REQ-01 (P0) — Единый бренд-модуль
`src/lib/brand.ts` экспортирует: два контекстных акцента (коралл для бирюзы, оранжевый для синего),
единый крем, сигнальный синий `#1400c3`, и функцию геометрии гексагона `hexPoints(cx, cy, r)`.
- **Критерий:** модуль существует; экспорты типизированы; OG-скрипты и (`через токены`) `global.css`
  согласованы с ним.

### REQ-02 (P0) — Drift-gate
`tests/lib/brand.test.ts` парсит токены `:root`, `[data-theme="light"]`, `[data-theme="cyberpunk"]`
из `src/styles/global.css` и падает, если значения расходятся с `brand.ts`.
- **Критерий (механический):** `bun test` падает при искусственном расхождении и зелёный при
  совпадении.

### REQ-03 (P0) — Баннеры без «голых» бренд-хексов
Все четыре OG-скрипта импортируют `brand.ts` вместо локальных `BLUE/ORANGE/CREAM`.
- **Критерий:** `grep -nE "#(1400c3|fe4e02|f8f2da|f4efca)" scripts/generate-*.mjs` — пусто
  (значения приходят из модуля).

### REQ-04 (P0) — CV-пара: стат-блок и артефакт
Убрать/заменить осознанным разделителем оранжевую линейку 120×3; вычистить стат-блок: единый
вертикальный ритм строк, разделители на уровне контейнера, достаточный контраст панели.
- **Критерий:** в SVG нет `<rect ... width="120" height="3">`; шаг строк выведен из одной константы;
  контраст value/label к фону панели ≥ AA.

### REQ-05 (P0) — Гексагон-портрет
Все четыре баннера используют единую `hexPoints()`; фото — на градиенте без кремовой заливки;
обрезка с нормальным запасом (голова не обрезана).
- **Критерий:** захардкоженных vertex-строк в скриптах нет; кремовая заливка «tile» отсутствует
  (`fill="none"`/градиент).

### REQ-06 (P0) — Глубина фона и декор
Фон баннеров получает градиент/vignette; декор-шестиугольники осмысленны (позиция, opacity).
- **Критерий:** в SVG присутствует градиентная/Vignette-заливка; декор — из `brand.ts`.

### REQ-07 (P1) — Бренд-марка и пилюля
Логотип NB и пилюля `CV · PDF` получают явную оптическую иерархию (размер, позиция, контраст).
- **Критерий:** контраст пилюли и логотипа ≥ AA; позиции выровнены по общей сетке.

### REQ-08 (P1) — Сетка, воздух, типографика баннеров
Краевые отступы, ритм колонки, иерархия Cormorant (имя) ↔ Inter (служебный текст), кернинг.
- **Критерий:** отступы кратны базовой шкале; имя — только Cormorant; служебный — только Inter.

### REQ-09 (P0) — Цифры героя
`HeroMetrics.astro`: `--font-mono` → Inter + `font-variant-numeric: tabular-nums`; лёгкие
разделители между метриками; единые единицы («п.п.»).
- **Критерий:** `HeroMetrics.astro` не использует `--font-mono`; разделители присутствуют; строки
  метрик не регрессируют по контрасту.

### REQ-10 (P0) — Вертикальный ритм героя
Смысловые блоки (аватар/H1, роль, питч+PhD, метрики, CTA) разделены разными интервалами.
- **Критерий:** разрыв между смысловыми блоками > разрыва внутри блока; правила в `global.css`.

### REQ-11 (P0) — CTA-ряд героя
Иерархия primary/secondary, отступы, мобильная упаковка (перенос/полная ширина).
- **Критерий:** на ≤480px кнопки не выходят за контейнер; primary визуально доминирует.

### REQ-12 (P1) — Аватар, правая панель, питч
Правки только если рендер показывает реальную проблему (оптический вес аватара, выравнивание
`.hero-visual`, длина строк питча).
- **Критерий:** решение зафиксировано в execution log; если правок нет — явно отмечено.

### REQ-13 (P0) — Чистка мёртвого CSS и компонента
Удалить `#intro-shader`-правила, `.hero-viz-*` и `IntroShader.astro`, если он нигде не импортируется.
- **Критерий:** `grep -rn "IntroShader\|hero-viz\|intro-shader" src/` — пусто; сборка зелёная.

### REQ-14 (P0) — Документация
Обновить `DESIGN.md` (крем/акценты/ссылка на бренд-модуль, если токены сдвинулись); при сдвиге
палитры — `docs/prd-palette-60-30-10.md`.
- **Критерий:** `DESIGN.md` и `global.css` согласованы; drift-тест зелёный.

### REQ-15 (P0) — Верификация
До/после рендеры героя (Playwright, по образцу `scripts/mobile-screenshot.mjs`), перегенерация
четырёх PNG, прогон команд §9.
- **Критерий:** все артефакты существуют; команды зелёные.

---

## 7. Technical Considerations

### 7.1 Как OG-скрипты получат `src/lib/brand.ts`
Скрипты — `.mjs` под Bun (`bun run scripts/...`), Bun резолвит `.ts`-импорты. Основной путь:
`import { BRAND } from '../../src/lib/brand.ts'`.
**Fallback (риск R1):** если импорт `.ts` из `.mjs` окажется нестабильным — `scripts/lib/brand.mjs`
как единственное место хранения, а `src/lib/brand.ts` делает ре-экспорт. Решение — на шаге 0.

### 7.2 Почему не кодогенерация `global.css`
`global.css` — 3366 строк рукописного CSS. Генерация блока токенов добавила бы билд-шаг и риск для
всего файла; выбран путь `brand.ts` + drift-тест (D13).

### 7.3 Контраст
Баннеры: `assertContrast` уже есть (`og-render.mjs:79–140`). Сайт: `contrast-gate.js` срабатывает
PostToolUse на правках `global.css` и охватывает три темы. Оранжевый `#fe4e02` на `#1400c3` = 3.47:1
— допустим только для large text (значения статов 34–40px/700); роль уже CREAM
(`prd-og-banner-restyle.md:61–64`).

### 7.4 Шрифты баннеров
Статические TTF в `scripts/og-fonts/`; рендер с `FONTCONFIG_FILE` + `PANGOCAIRO_BACKEND=fc` и
`cwd = scripts/og-fonts` (иначе CoreText игнорирует fontconfig). Не менять (D3-инвариант
исполненного PRD).

---

## 8. Work Plan

**Шаг 0 — спайк импорта.** Проверить `import` `brand.ts` из `scripts/*.mjs` под Bun. Блокирует
REQ-01/REQ-03.

**Шаг 1 — фундамент.** `src/lib/brand.ts` (хексы, `hexPoints`), `tests/lib/brand.test.ts`
(drift-gate). REQ-01, REQ-02.

**Шаг 2 — герой.** `HeroMetrics.astro` (REQ-09), `global.css` (REQ-10, REQ-11, REQ-13),
`index.astro` + `en/index.astro` при необходимости структуры (REQ-10/11). REQ-12 — по рендеру.

**Шаг 3 — баннеры.** `generate-cv-og.mjs` и `generate-cv-linkedin.mjs` глубоко (REQ-03…REQ-08);
`generate-home-og.mjs` и `generate-graph-og.mjs` точечно (REQ-03, REQ-05). Перегенерация PNG.

**Шаг 4 — доки.** `DESIGN.md`, при необходимости `docs/prd-palette-60-30-10.md`. REQ-14.

**Шаг 5 — проверка.** Рендеры до/после героя; превью баннеров 1200 и ~500 px; контраст-таблицы;
`bun run check`, `bun test`, LHCI. REQ-15.

---

## 9. Acceptance Metrics

| # | Критерий | Тип |
|---|---|---|
| A1 | `tests/lib/brand.test.ts` ловит расхождение токенов с `brand.ts` и зелёный при совпадении | механический |
| A2 | `grep -nE "#(1400c3\|fe4e02\|f8f2da\|f4efca)" scripts/generate-*.mjs` — пусто | механический |
| A3 | захардкоженных hexagon-vertex-строк в скриптах нет | механический |
| A4 | нет `<rect ... width="120" height="3">` в CV-скриптах | механический |
| A5 | `bun run og:cv`, `og:cv:li`, `og:home`, `og:graph` — exit 0, контраст-ассерт зелёный | механический |
| A6 | `grep -rn "IntroShader\|hero-viz\|intro-shader" src/` — пусто | механический |
| A7 | `HeroMetrics.astro` не использует `--font-mono` | механический |
| A8 | `bun run check` зелёный | механический |
| A9 | `bun test` зелёный | механический |
| A10 | LHCI: a11y/SEO/BP ≥ 0.95, perf ≥ 0.85 | механический |
| A11 | contrast-gate hook зелёный на правках `global.css` | механический |
| A12 | герой и баннеры читаются как серия (цифры, единицы, ритм) | ручной |
| A13 | CV-баннер без визуальных артефактов на превью 1200 и ~500 px | ручной |
| A14 | `git diff src/lib/graph*.ts src/lib/topics.ts` — пусто | механический |

---

## 10. Risks

| Риск | Вероятность | Влияние | Митигация | Контингенция |
|---|---|---|---|---|
| R1 — Bun не импортирует `.ts` из `.mjs` стабильно | Низкая | Medium | Шаг 0 блокирует | `scripts/lib/brand.mjs` + ре-экспорт из `src/lib/brand.ts` (§7.1) |
| R2 — Выравнивание крема ломает AA на синем | Низкая | Medium | Контраст-ассерт в скриптах | Вернуть баннерный крем `#f8f2da` (PENDING P1) |
| R3 — Правки `global.css` блокирует contrast-gate | Средняя | Low | Проверять пары до записи; смотреть вывод hook | Скорректировать токен, не обходить hook |
| R4 — Полировка героя задевает 3 темы непредсказуемо | Средняя | Medium | Рендеры во всех трёх темах | Откат конкретного правила |
| R5 — Дрейф PNG: перегенерированные баннеры не закоммичены | Средняя | Low | Явно сообщить список изменённых PNG | Не коммитить без команды (D3) |
| R6 — Удаление `IntroShader.astro` ломает неожиданный импорт | Низкая | Low | `grep` перед удалением (REQ-13) | Оставить компонент, удалить только CSS |
| R7 — LHCI perf регрессирует от правок CSS | Низкая | Medium | Прогон LHCI до/после | Откат правила |

---

## 11. PENDING — нужно от владельца

- **P1 (важно):** выравнивание крема до одного значения (кандидат — светлый фон сайта `#f4efca`)
  **пересматривает** `prd-og-banner-restyle.md` §4 («палитра баннеров остаётся `#f8f2da`»).
  Требуется явное подтверждение: (A) выравнивать до `#f4efca`; (B) оставить `#f8f2da` и выровнять
  сайт; (C) не выравнивать, жить с двумя кремами, зафиксировав оба в `brand.ts`.
- **P2:** глубина правок home/graph — они отрестайлены сегодня. Рекомендация: точечно (единый
  гексагон + бренд-импорт), композицию не трогать.
- **P3:** разрешение на коммит (по D3/D7 — только по явной команде).
- **P4:** создавать ли vault-заметку-дубль PRD (по образцу `prd-palette-60-30-10.md`)? По умолчанию — нет.

---

## 12. Appendix — инвентарь файлов

### Новые
| Путь | Что |
|---|---|
| `src/lib/brand.ts` | хексы (два акцента, крем, синий), `hexPoints()` |
| `tests/lib/brand.test.ts` | drift-gate токенов `global.css` ↔ `brand.ts` |
| `scripts/lib/brand.mjs` (`[P]`, только при провале R1) | резервный носитель бренда |

### Изменяемые
| Файл | Что |
|---|---|
| `scripts/generate-cv-og.mjs` | REQ-03…REQ-08 — основная правка CV |
| `scripts/generate-cv-linkedin.mjs` | REQ-03…REQ-08 |
| `scripts/generate-home-og.mjs` | REQ-03, REQ-05 (точечно) |
| `scripts/generate-graph-og.mjs` | REQ-03, REQ-05 (точечно) |
| `src/components/HeroMetrics.astro` | REQ-09 |
| `src/styles/global.css` | REQ-10, REQ-11, REQ-13, токены (REQ-14) |
| `src/pages/index.astro`, `src/pages/en/index.astro` | REQ-10/11 (при необходимости структуры) |
| `DESIGN.md` | REQ-14 |
| `docs/prd-palette-60-30-10.md` | REQ-14 (при сдвиге палитры) |

### Удаляемые
| Путь | Что |
|---|---|
| `src/components/IntroShader.astro` | REQ-13, только если нет импортов |
| `global.css` строки `#intro-shader`, `.hero-viz-*` | REQ-13 |

### Генерируемые артефакты
| Путь | Что |
|---|---|
| `public/images/og/cv-banner.png` | перезапись |
| `public/images/og/cv-linkedin.png` | перезапись |
| `public/images/og/portfolio-banner-v2.png` | перезапись (точечно) |
| `public/images/og/portfolio-graph-v2.png` | перезапись (точечно) |

### Не трогаем
`src/lib/graph.ts`, `src/lib/graph-layout.ts`, `src/lib/topics.ts`, `graph.json`, `/graph/`,
`scripts/generate-og.mjs`, постовые OG, `src/layouts/Base.astro`, `.lighthouserc.json`,
`scripts/og-fonts/`, `scripts/lib/og-render.mjs` (используется как есть).

---

## 13. Проверенные факты, на которые опирается документ `[V]`

- Рендерер — `rsvg-convert 2.62.3` через `scripts/lib/og-render.mjs`; шрифты — статические TTF в
  `scripts/og-fonts/` с `FONTCONFIG_FILE` + `PANGOCAIRO_BACKEND=fc`.
- Палитра баннеров захардкожена: `BLUE #1400c3`, `ORANGE #fe4e02`, `CREAM #f8f2da`; graph
  дополнительно `MUTED #bdb5ea`, `PERI #9db4ff`, `PEACH #ffb38a`.
- Сайт: `--text-accent #ff8569` (dark/cyberpunk), `#a8331a` (light); `--button-bg #1400c3`;
  light `--background-primary #f4efca`.
- `#fe4e02` не встречается в `src/`; крем `#f8f2da` ≠ `#f4efca`.
- Геометрия гексагона: в `cv-og`/`home`/`graph` — захардкоженные вершины; в `cv-linkedin` —
  вычисляемая (`hex()`).
- Цифры героя — `--font-mono`; разделителей между метриками нет.
- Мёртвый CSS/компонент: `#intro-shader`, `.hero-viz-cards`, `.hero-viz-card`, `IntroShader.astro`.
- Артефакт-линейка 120×3 px в `generate-cv-og.mjs:139` и `generate-cv-linkedin.mjs:152`.
- Тесты — `bun test tests/lib` (18 файлов, `bunfig.toml` preload); типы — `bun run check`;
  LHCI — `.lighthouserc.json`; contrast-gate — `.claude/hooks/contrast-gate.js`.
- `prd-og-banner-restyle.md` исполнен 2026-09-19 (graph/home переписаны, шрифты вендорены,
  `portfolio-graph-v1.png` удалён, коммит не делался).

---

## 14. Execution log

**Дата исполнения:** 2026-09-19. **Статус:** REQ-01…REQ-15 выполнены; коммит — за владельцем (D3/P3).

### Принятые по ходу решения

| Точка | Решение |
|---|---|
| Шаг 0 (R1) | Риск не материализовался: `scripts/generate-graph-og.mjs` уже импортирует `../src/lib/graph.ts` из `.mjs` под Bun. Импорт `../src/lib/brand.ts` — тот же путь, `scripts/lib/brand.mjs` не понадобился |
| P1 | **(A)** крем выравнен до `#f4efca` (значение light-темы). `#f8f2da` из баннеров ушёл; `prd-og-banner-restyle.md` §4 аннотирован как пересмотренный |
| P2 | home/graph — **точечно**: импорт `brand.ts`, единые `hexPoints`/`hexPointsFlat`, замена кремовой плитки на тёплый гало. Сетка, кернинг и композиция не тронуты |
| Гексагон бренд-марки | Nav-логотип — **flat-top** (`Base.astro:189`), портрет — **pointy-top**. Чтобы не менять бренд-марку, в `brand.ts` две функции: `hexPoints` (портрет) и `hexPointsFlat` (NB-марка, парность с nav). В CV-паре логотип дополнительно прижат левой вершиной к `EDGE`, устраняя оптический сдвиг на 5.6 px |
| REQ-04, артефакт | Линейка `120×3` **удалена** без замены: при выровненном вью от неё остаётся «случайный штрих». В home/graph аналогичная линейка оставлена (P2, вне scope) |
| REQ-05, кремовая плитка | Плитка (покрытая кольцом) заменена тёплым гало `stroke accent, opacity 0.14` в CV-паре и home/graph. `fill="none"` — кремовой заливки «tile» нет |
| REQ-09, единицы | `pp` → `" п.п."` (RU) с пробелом, `K/yr` → `K/год` (RU). EN: `" pp"`, `K/yr` |

### Что изменено

- **Новые:** `src/lib/brand.ts` (палитра + `hexPoints`/`hexPointsFlat`), `tests/lib/brand.test.ts` (drift-gate, 3 теста).
- **Hero:** `HeroMetrics.astro` (Inter + `tabular-nums`, разделители, единицы), `global.css` (ритм `.hero-body`/интер-блоки, CTA-иерархия + full-width на ≤480px, удаление мёртвого CSS). Структура `index.astro`/`en/index.astro` **не потребовалась** — REQ-10/11 решены правилами CSS.
- **Баннеры:** все четыре генератора импортируют `brand.ts`; CV-пара переписана глубоко (сетка `EDGE + n·UNIT`, единая `ROW_H`, разделители на уровне контейнера, тёмный колодец scorecard, halo), home/graph — точечно.
- **Пятый артефакт (вне исходного scope, по подтверждению владельца):** `scripts/generate-cv-cover.mjs` — A4-обложка CV, появилась параллельной сессией. Дотянута до `brand.ts` тем же паттерном: старый крем `#f8f2da` ушёл, `hexPoints`/`hexPointsFlat` вместо захардкоженных вершин. Без этого обложка в PDF и OG-баннер той же ссылки показывали разные кремы — тот самый дрейф, который PRD устраняет. `cv-cover.pdf` пересобран, `cv-cover.png` перерендерен с него (300 dpi).
- **Удалено:** `src/components/IntroShader.astro` (импортов нет — grep чист), `#intro-shader`, `.hero-viz-*`.

### Acceptance metrics

| # | Критерий | Результат |
|---|---|---|
| A1 | drift-gate ловит расхождение и зелёный при совпадении | ✅ 3/3 pass; 2 теста доказывают, что gate «кусается» |
| A2 | нет бренд-хексов в `generate-*.mjs` | ✅ grep пуст — включая `generate-cv-cover.mjs` (после дотяжки) |
| A3 | нет захардкоженных hexagon-vertex-строк | ✅ grep `points="[0-9]` пуст по всем `generate-*.mjs` |
| A4 | нет `<rect ... width="120" height="3">` в CV-скриптах | ✅ grep пуст |
| A5 | `og:cv`, `og:cv:li`, `og:home`, `og:graph` — exit 0, ассерт зелёный | ✅ все 4 зелёные (CV-пара: стат-токены проверены против `#0a0070`, 5.06:1 / 14.47:1) |
| A6 | `grep -rn "IntroShader\|hero-viz\|intro-shader" src/` пуст | ✅ пуст |
| A7 | `HeroMetrics.astro` без `--font-mono` | ✅ grep пуст |
| A8 | `bun run check` зелёный | ✅ 0 errors, 0 warnings, 0 hints (122 файла) |
| A9 | `bun test` зелёный | ✅ 154 pass / 0 fail (19 файлов) |
| A10 | LHCI: a11y/SEO/BP ≥ 0.95, perf ≥ 0.85 | ⚠️ a11y 0.98, bp 1.00, seo 1.00 — ✅. perf: home 0.55→**0.66**, en 0.61, writing 0.92, volta 0.98, topics 0.99. Home/en ниже 0.85 **до и после** (baseline home 0.64/en 0.63): TBT 2.5–4.5 s создаёт 3D-аватар, разброс между прогонами ±1 s; **CLS идентичен 0.052 до/после** — то есть layout-правки не сдвинули метрику. Регрессии нет |
| A11 | contrast-gate hook зелёный на `global.css` | ✅ exit 0 |
| A12 | герой и баннеры читаются как серия | ✅ ручная сверка рендеров: Inter + `tabular-nums` в обоих, единые `п.п.`/`€656K`, разделители в обоих |
| A13 | CV-баннер без артефактов на 1200 и ~500 px | ✅ ручная сверка (см. превью 500 px): линейки нет, scorecard читается |
| A14 | `git diff src/lib/graph*.ts src/lib/topics.ts` пуст | ✅ пуст |

### Открытые вопросы / честные оговорки

1. **Фото-фон.** Кремовый вид внутри гексагона даёт не «tile», а светлый фон самой фотографии (`00_profile.jpg`). Плитка геометрически скрыта кольцом; её замена видима только как halo. Полное устранение «кремового гексагона» требует вырезанного портрета — вне scope (§4, новых ассетов не добавляем).
2. **`@paper-design/shaders`** остался в `package.json` без потребителя после удаления `IntroShader.astro`. Удаление зависимости — за рамками PRD (не добавляем/не удаляем зависимости); оставлено как есть.
3. **Перф.** `perf ≥ 0.85` не выполнялся и до этой работы (см. A10).
4. **PNG.** Перегенерированы все четыре баннера (`cv-banner`, `cv-linkedin`, `portfolio-banner-v2`, `portfolio-graph-v2`).
5. **Посторонние правки в дереве.** В процессе работы в этом же каталоге параллельная сессия вела задачу CV-cover (`scripts/generate-cv-cover.mjs`, `scripts/sync-cv-pdf.mjs`, `scripts/lib/og-render.mjs` + `sharp`). Параллельная сессия закоммитила рабочее дерево целиком одним коммитом `db259a4` — в него попали и мои правки, хотя D3/P3 предполагали коммит только по явной команде владельца. Файлы `sync-cv-pdf.mjs` и `og-render.mjs` я не трогал; `generate-cv-cover.mjs` дотянут до `brand.ts` по отдельному подтверждению владельца. Незакоммиченными остались: execution log этого PRD, `generate-cv-cover.mjs` + пересобранные `cv-cover.pdf`/`.png`, и фикс `" п.п."` с пробелом в `HeroMetrics.astro`.

---

**Конец PRD**

*Файл: `docs/prd-hero-banner-brand-polish.md`.*
